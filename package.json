{
  "name": "website",
  "private": true,
  "dependencies": {},
  "devDependencies": {
    "node-sass": "^7.0.0"
  },
  "scripts": {
    "build": "./build.sh",
    "deploy": "./deploy.sh",
    "dev": "node-sass --watch public/styles/sass -o public/styles --output-style compressed"
  }
}
